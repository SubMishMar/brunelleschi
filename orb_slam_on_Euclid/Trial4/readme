orbfeatures = 1000.
hand held
Faster and more complicated motion

For Luenberger Estimator
A = [zeros(3,3) eye(3,3);
     zeros(3,3) zeros(3,3)];
 
B = [zeros(3,3);
     eye(3,3)];

C = [eye(3,3) zeros(3,3)];

p = [-10 -10 -10 -100 -100 -100];

L = place(A', C', p)';
